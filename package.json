{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "android:add-expo": "pnpm --filter @workspace/catalog-scanner-android add expo",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "expo": "^57.0.11"
  },
  "devDependencies": {
    "@types/react": "~19.2.4",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
